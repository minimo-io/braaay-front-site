{
    "name": "braaay-tailwind-base",
    "version": "1.0.0",
    "description": "Intended for development purposes only. The idea of this sub-project is to generate the minified and unminified versions of the css packages for Braaay.com Yo be used as the css library for Svelte (mobile & desktop) & Woocommerce front-ends.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "minify": "lightningcss --minify --bundle --targets '>= 0.25%' ./src/style.css -o ./src/style.min.css",
        "minify:tailwind": "tailwindcss -o build.css --minify",
        "tw": "tailwindcss -i ./src/style.css -o ./src/bry.build.css  --minify --watch"
    },
    "author": "nostr:npub1wujhdsytm3w6g0mpsqh8v7ezx83jcm64dlkwuqgm5v8lv0pds55ssudkw0",
    "license": "ISC",
    "devDependencies": {
        "@tailwindcss/aspect-ratio": "^0.4.2",
        "lightningcss-cli": "^1.26.0",
        "live-server": "^1.2.2",
        "tailwindcss": "^3.4.10"
    }
}
